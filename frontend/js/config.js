// API base URL for fetch requests.
// Leave as empty string for same-origin (served by backend at http://localhost:3000).
// Set to 'http://localhost:3000' when serving frontend separately (e.g., Live Server).
const API_BASE = '';
